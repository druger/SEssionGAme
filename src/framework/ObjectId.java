package framework;

public enum ObjectId { //enum(перечисление)- обеспечивает для переменной, набор предопределенных констант
    
    Player(),
    PlayerGun(),
    Teacher(),
    Flag(),
    Block(),
    Bullet();
}
